
function sumar(a, b) {//funcion para sumar
    return a + b;
}

function restar(a, b) {//funcion para restar
    return a - b;
}

module.exports = {//exportar las funciones sumar y restar
    sumar,
    restar
};