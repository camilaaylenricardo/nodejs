class Calculadora {//crear clase calculadora donde a y b se multiplique y se divida.
    multiplicar(a, b) {
        return a * b;
    }

    dividir(a, b) {
        return a / b;
    }
}

export default Calculadora;//exportar por defecto calculadora